# 真机回归清单（发布前 / 升级后）

按此清单在真实设备上逐项打勾。每项给出操作 → 预期结果 → 控制台验证（可选）。

## 0. 前置确认

| # | 操作 | 预期 |
|---|---|---|
| 0.1 | 硬刷新页面 | 页面正常渲染（无失败卡片） |
| 0.2 | `document.body.getAttribute('data-dsh-mobile-theme')` | `"0.5.0"` |
| 0.3 | `document.body.getAttribute('data-dsh-mobile-layout')` | `"ok"`（结构自标记成功） |

建议环境矩阵：**Android Chrome**、**iOS Safari**、**微信内置浏览器（X5）** 至少各走一遍 §1-§3；其余项在任意一台设备过一遍即可。

## 1. 布局（竖屏 ≤767px）

| # | 操作 | 预期 |
|---|---|---|
| 1.1 | 打开页面 | 会话区真全宽，无左侧图标轨 |
| 1.2 | 观察头部 | 左上仅 ☰、标题紧凑、工具区可横滚 |
| 1.3 | 观察输入条 | 底部工具栏单行；发送键固定在右下 |
| 1.4 | 打开设置 | 先 ☰ 展开抽屉，再点抽屉底部设置；面板与抽屉同宽，不要铺满屏幕；顶部导航横排可滚 |
| 1.5 | 打开目录选择器 | 全屏 sheet、40px 行高、底部取消/确定同行 |
| 1.6 | 打开工具调用详情（检查按钮） | 触屏上「查看」按钮常显 |
| 1.7 | 查看代码块复制按钮 | 图标与行对齐（不偏移），点击区域约 30×30 |
| 1.8 | 横屏（≤900×500） | 紧凑纵向节奏；☰ 与抽屉可用 |
| 1.9 | 观察用户消息 | 发送时间常显（小字，永不截断、点击不触发显示/隐藏）；助手消息轻点显示时间/统计（省略号截断）、4 秒自动隐藏、再点隐藏、点按钮不触发 |

## 2. 抽屉交互

| # | 操作 | 预期 |
|---|---|---|
| 2.1 | 点 ☰ | 抽屉从左侧滑入 + 蒙层出现 |
| 2.2 | 点右侧空白遮罩 | 抽屉收起 |
| 2.3 | 点抽屉内搜索输入框 | **保持打开**，可正常输入 |
| 2.4 | 点设置（齿轮） | **保持打开**，设置面板弹出 |
| 2.5 | 在设置内点任意选项/popover | **抽屉始终不动** |
| 2.6 | 点一个会话 | 抽屉收起、回到对话 |
| 2.7 | 点搜索结果 | 抽屉收起、跳到会话 |
| 2.8 | 点新建会话 | 抽屉收起 |
| 2.9 | 点项目/工作区行 | **保持打开**（展开不收起） |
| 2.10 | 抽屉开着按 Esc（外接键盘） | 收起 |
| 2.11 | 抽屉开着按系统返回手势 | 收起（且不影响应用其他历史） |
| 2.12 | 抽屉内点收起按钮 | 收起，无双重切换 |

## 3. 键盘与发送

| # | 操作 | 预期 |
|---|---|---|
| 3.1 | 首次加载页面 | 键盘**不**自动弹出 |
| 3.2 | 点输入框打字 | 键盘弹出，输入条被顶到键盘上方、无底部空白带 |
| 3.3 | 键盘弹出时滚动到底 | 内容恰好止于输入条，「回到底部」按钮不脱位 |
| 3.4 | 发送一条消息 | 键盘保持打开、上一条消息末尾的工具信息可见、无「回到底部」悬浮按钮遮挡 |
| 3.5 | 收起键盘 | 布局完全复原、「回到底部」悬浮按钮消失、滚动位置正常 |
| 3.6 | 从抽屉切换会话 | 键盘**不**弹出 |
| 3.7 | 键盘开着捏合缩放页面 | 布局不跳动（无键盘时缩放不影响） |

## 4. DSH 0.1.5 新版与升级回归

| # | 操作 | 预期 |
|---|---|---|
| 4.1 | 右上角打开文件 Sidebar，预览 Markdown / 图片 / PDF | 手机全屏，标签切换、文件操作、关闭入口可见 |
| 4.2 | 右侧面板打开时观察 `data-rightbar-collapsed` | 即使网格轨道收起，实际预览仍可见且能用系统返回关闭 |
| 4.3 | 横屏手机打开右侧 Sidebar | 全屏覆盖，主对话仍保持全宽，关闭后恢复 |
| 4.4 | 新输入框输入中文、组合输入、粘贴、发送附件 | Lexical 内容正确，输入法不被焦点守卫打断 |
| 4.5 | 点赞/点踩打开反馈，输入内容 | 对话框在视口内；失败提示和草稿保留由官方处理 |
| 4.6 | 设置内切换语言 | 菜单按钮 aria-label 随语言切换，原主题偏好保持 |
| 4.7 | `npm run deploy:hot` | 无需刷新，只有一个样式标签与菜单按钮；菜单、返回、键盘功能继续工作 |
| 4.8 | 在测试构建中改变 CSS 模块哈希 | 同名模块/局部名称仍可解析，不丢失触控样式 |
| 4.9 | 在测试构建中移除或重命名 main/rightbar Slot | 旧结构标记撤销，诊断 degraded，官方布局及原生菜单可用 |
| 4.10 | 打开设置和目录弹窗 | ☰ 隐藏，不遮挡弹窗标题或操作 |

自动化覆盖：`npm test`、`npm run check:upstream`、`npm run test:browser`。浏览器脚本使用临时 home 和工作区，不读取用户现有 DSH 配置；缺少凭据的预期失败用于建立测试会话，不调用真实模型。真机检查项仍需单独勾选。

## 5. 记录模板

```
设备/浏览器：________  视口：________  版本标记：v0.5.0
§0 全部通过：□   §1 全部通过：□   §2 全部通过：□   §3 全部通过：□
失败项编号与现象：
```
