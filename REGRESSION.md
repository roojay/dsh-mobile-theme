# 真机回归清单（发布前 / 升级后）

按此清单在真实设备上逐项打勾。每项给出操作 → 预期结果 → 控制台验证（可选）。

## 0. 前置确认

| # | 操作 | 预期 |
|---|---|---|
| 0.1 | 硬刷新页面 | 页面正常渲染（无失败卡片） |
| 0.2 | `document.body.getAttribute('data-dsh-mobile-theme')` | `"0.3.18"` |
| 0.3 | `document.body.getAttribute('data-dsh-mobile-layout')` | `"ok"`（结构自标记成功） |

建议环境矩阵：**Android Chrome**、**iOS Safari**、**微信内置浏览器（X5）** 至少各走一遍 §1-§3；其余项在任意一台设备过一遍即可。

## 1. 布局（竖屏 ≤767px）

| # | 操作 | 预期 |
|---|---|---|
| 1.1 | 打开页面 | 会话区真全宽，无左侧窄轨 |
| 1.2 | 观察头部 | 左侧给 ☰ 预留槽位、标题紧凑、工具区可横滚 |
| 1.3 | 观察输入条 | 底部工具栏单行；发送键固定在右下 |
| 1.4 | 打开设置 | 全屏 sheet（`100dvh`），分区导航横向可滚 |
| 1.5 | 打开目录选择器 | 全屏 sheet、40px 行高、底部取消/确定同行 |
| 1.6 | 打开工具调用详情（检查按钮） | 触屏上「查看」按钮常显 |
| 1.7 | 查看代码块复制按钮 | 图标与行对齐（不偏移），点击区域约 30×30 |
| 1.8 | 横屏（≤900×500） | 紧凑纵向节奏；☰ 与抽屉可用 |
| 1.9 | 观察用户消息 | 发送时间常显（小字，永不截断、点击不触发显示/隐藏）；助手消息轻点显示时间/统计（省略号截断）、4 秒自动隐藏、再点隐藏、点按钮不触发 |

## 2. 抽屉交互

| # | 操作 | 预期 |
|---|---|---|
| 2.1 | 点 ☰ | 抽屉从左侧滑入 + 蒙层出现 |
| 2.2 | 点右侧空白遮罩 | 抽屉收起 |
| 2.3 | 点抽屉内搜索输入框 | **保持打开**，可正常输入 |
| 2.4 | 点设置（齿轮） | **保持打开**，设置面板弹出 |
| 2.5 | 在设置内点任意选项/popover | **抽屉始终不动** |
| 2.6 | 点一个会话 | 抽屉收起、回到对话 |
| 2.7 | 点搜索结果 | 抽屉收起、跳到会话 |
| 2.8 | 点新建会话 | 抽屉收起 |
| 2.9 | 点项目/工作区行 | **保持打开**（展开不收起） |
| 2.10 | 抽屉开着按 Esc（外接键盘） | 收起 |
| 2.11 | 抽屉开着按系统返回手势 | 收起（且不影响应用其他历史） |
| 2.12 | 抽屉内点收起按钮 | 收起，无双重切换 |

## 3. 键盘与发送

| # | 操作 | 预期 |
|---|---|---|
| 3.1 | 首次加载页面 | 键盘**不**自动弹出 |
| 3.2 | 点输入框打字 | 键盘弹出，输入条被顶到键盘上方、无底部空白带 |
| 3.3 | 键盘弹出时滚动到底 | 内容恰好止于输入条，「回到底部」按钮不脱位 |
| 3.4 | 发送一条消息 | 键盘保持打开、上一条消息末尾的工具信息可见、无「回到底部」悬浮按钮遮挡 |
| 3.5 | 收起键盘 | 布局完全复原、「回到底部」悬浮按钮消失、滚动位置正常 |
| 3.6 | 从抽屉切换会话 | 键盘**不**弹出 |
| 3.7 | 键盘开着捏合缩放页面 | 布局不跳动（无键盘时缩放不影响） |

## 4. 升级韧性（可选，模拟）

| # | 操作 | 预期 |
|---|---|---|
| 4.1 | 控制台手动删除侧栏标记：`document.querySelector('[data-dsh-mobile-sidebar-col]')?.removeAttribute('data-dsh-mobile-sidebar-col')` 后刷新 | 布局回到官方窄轨模式、功能完好、`data-dsh-mobile-layout` 仍为 ok（重试自愈） |

## 5. 记录模板

```
设备/浏览器：________  视口：________  版本标记：v0.3.18
§0 全部通过：□   §1 全部通过：□   §2 全部通过：□   §3 全部通过：□
失败项编号与现象：
```
